# Tasks — [Feature name]

> Feature: `NNN-slug` · Based on: [plan.md](./plan.md) · Created: [YYYY-MM-DD]

Ordered, **very small, independently verifiable** tasks. Check each off as it is
completed.

- [ ] **T1** — [single concrete change] · files: `…` · check: [test/behavior that proves it]
- [ ] **T2** — … · files: `…` · check: …
- [ ] **T-tests** — add/adjust tests for [logic] · check: `npm test` covers it
- [ ] **T-DoD** — Definition of Done: `npx tsc --noEmit` + `npm run lint` + `npm test` + `npm run build` green; manual check on [route]

## Traceability

| Task | Acceptance criteria |
| ---- | ------------------- |
| T1   | AC1                 |
