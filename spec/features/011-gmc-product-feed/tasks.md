# Tasks — Google Merchant Center product feed (token-protected XML)

> Feature: `011-gmc-product-feed` · Based on: [plan.md](./plan.md) · Created: 2026-07-08

Ordered, **very small, independently verifiable** tasks. Check each off as done.

- [x] **T1** — Extract the storefront filter to a shared constant. In `tiresRepository.ts`, add `export const STOREFRONT_SELLABLE_WHERE = "Local = '0' AND Trash = 'false' AND Condition != 'sold' AND RemainingLife >= '50%' AND Price != 0"` and use it at line 185 (`fetchTires`) — **identical string**, pure refactor. · files: `src/repositories/tiresRepository.ts` · check: `tsc` green; `npm test` unchanged (zero behaviour change); constant string equals the old inline clause.
- [x] **T2** — Add the feed data-access. In `tiresRepository.ts` add `type FeedTireRecord` (whitelisted cols: `TireId, Code, Brand, Model2, RealSize, Price, ProductTypeId, Condition, RemainingLife, Patched, Tread, loadIndex, speedIndex, Image1..4`), a pure `buildFeedQuery()` returning `SELECT <whitelist> FROM dbo.View_Tires WHERE ${STOREFRONT_SELLABLE_WHERE} ORDER BY ModificationDate DESC` (no `TOP`/OFFSET), and `fetchSellableTiresForFeed(): Promise<FeedTireRecord[]>` using `getPool`/`logQuery`. · files: `src/repositories/tiresRepository.ts` · check: `buildFeedQuery()` contains `STOREFRONT_SELLABLE_WHERE` and no cap; `tsc` green.
- [x] **T3** — Feed helper constants & types. New `src/app/utils/merchantFeed.ts`: `FEED_REVALIDATE_SECONDS = 43200`, `GOOGLE_PRODUCT_CATEGORY_TIRES = '912'`, and `type GmcItem` = explicit whitelist of output fields (`id, title, description, link, imageLink, additionalImageLink, price, availability, condition, brand, googleProductCategory, productType, identifierExists`). `additionalImageLink` is a **`string[]`** (GMC emits one `<g:additional_image_link>` per image, up to 10). · files: `src/app/utils/merchantFeed.ts` · check: `tsc` green; type compiles.
- [x] **T4** — `escapeXml` + `buildFeedTitle`. In `merchantFeed.ts`: `escapeXml(s)` (escape `& < > " '`) and `buildFeedTitle({brand,size,condition,remainingLife})` → `Michelin 225/40R18 Used Tire — 80% tread` (omit tread bit if absent/new). · files: `src/app/utils/merchantFeed.ts` · check: unit tests for escaping + title format (see T8).
- [x] **T5** — `buildFeedItem(record)` → `GmcItem`. Map a `FeedTireRecord`: title (T4), description (`generateTireDescription` fallback when DB desc empty), `link` via `buildTireSlug`+`absUrl`, `imageLink`=first image + `additionalImageLink`=**array** of the rest (absolutized; generic fallback if none), `price` `"80.00 USD"`, `availability='in_stock'`, `condition` `new`/`used` from `ProductTypeId`, `brand`, `googleProductCategory=912`, `productType='Tires > {Brand} > {Size}'`, `identifierExists='no'`. Emit **only** the whitelist keys. · files: `src/app/utils/merchantFeed.ts` · check: unit asserts fields + formats + `Object.keys` == whitelist (T8).
- [x] **T6** — `buildMerchantFeedXml(items)` + `isValidFeedToken`. RSS 2.0 string with `xmlns:g="http://base.google.com/ns/1.0"`, `<channel>` + one `<item>` per `GmcItem` (all values `escapeXml`'d, `g:` namespaced); `additionalImageLink[]` → one repeated `<g:additional_image_link>` per entry. `isValidFeedToken(provided, expected)` — length-guarded `crypto.timingSafeEqual`, returns boolean. · files: `src/app/utils/merchantFeed.ts` · check: output parses as XML, contains `xmlns:g` + N items; token compare unit tests (T8).
- [x] **T7** — Route handler. New `src/app/feed/google-merchant.xml/route.ts` (route `/feed/google-merchant.xml`), wrapped in `withLogging`: Zod-validate `?key=`; `isValidFeedToken` vs `process.env.MERCHANT_FEED_TOKEN` (missing env → 503; bad/no key → 403); fetch via `unstable_cache(fetchSellableTiresForFeed, ['merchant-feed'], { revalidate: FEED_REVALIDATE_SECONDS, tags: ['tires'] })`; serialize; return `Response` with `Content-Type: application/xml; charset=utf-8` + `Cache-Control: public, s-maxage=43200, stale-while-revalidate=86400`; try/catch → generic 500 (log via `logger.error`, no leak). **Route-convention exception:** this handler lives outside `app/api/*` and returns XML (not JSON) — a deliberate deviation from `tech-stack.md:70` for a clean, GMC-friendly URL (and `/api` is robots-disallowed); precedent exists (`sitemap.ts`, `robots.ts`, `opengraph-image`). **Fallback:** if the dotted segment `google-merchant.xml/` fails to build, use `/feed/google-merchant` (no extension — GMC doesn't require `.xml`); the T-DoD build confirms it. · files: `src/app/feed/google-merchant.xml/route.ts` · check: manual `curl` with/without token; route tests (T10).
- [x] **T8** — Not discoverable. Add `'/feed/'` to the `disallow` array in `robots.ts`; confirm `sitemap.ts` does not reference the feed. · files: `src/app/robots.ts` · check: `robots()` output disallows `/feed/`; sitemap source has no `google-merchant`.
- [x] **T9** — Unit tests for helpers. New `src/app/utils/merchantFeed.test.ts`: title format, `escapeXml`, `buildFeedItem` required attrs + formats + whitelist keys (AC3/AC4), `buildMerchantFeedXml` well-formed + item count (AC1), `isValidFeedToken` right/wrong/empty (AC5), `buildFeedQuery` reuses storefront clause & not dashboard & no cap (AC2), and assertions that `robots.ts` disallows `/feed/` and `sitemap.ts` omits the feed (AC8). · files: `src/app/utils/merchantFeed.test.ts` · check: `npm test` green.
- [x] **T10** — Route tests. New `src/app/feed/google-merchant.xml/route.test.ts`. **Mock `next/cache` so `unstable_cache` is passthrough** (`vi.mock('next/cache', () => ({ unstable_cache: (fn) => fn }))`) — otherwise it needs Next's request context and won't run in vitest — and mock `fetchSellableTiresForFeed`. Assert: no key→403, wrong key→403, correct key→200 + `application/xml` + `Cache-Control` present (AC5/AC7), forced fetch throw→500 with no stack/secret in body (AC6). Set `process.env.MERCHANT_FEED_TOKEN` in the test. · files: `src/app/feed/google-merchant.xml/route.test.ts` · check: `npm test` green.
- [x] **T11** — Docs. Add `MERCHANT_FEED_TOKEN` (server-only) to `.env.example` (if present) with a comment; update `spec/roadmap.md`: add the GMC feed item (🟡/✅ as fits) and a backlog line for the **public-API security review**. · files: `.env.example`, `spec/roadmap.md` · check: env documented; roadmap reflects both items.
- [x] **T-DoD** — Definition of Done: `npx tsc --noEmit` + `npm run lint` + `npm test` + `npm run build` all green. **Manual check:** `curl '/feed/google-merchant.xml'` → 403; with `?key=<token>` → 200 valid XML; spot-check an `<item>` (title Brand+Size+Condition, absolute `link`/`image_link`, `g:condition`, `g:google_product_category=912`, `g:identifier_exists=no`); confirm a sold/local/unpriced tire is absent. Leave changes **staged, not committed**, until the owner confirms.

## Traceability

| Task        | Acceptance criteria |
| ----------- | ------------------- |
| T1, T2      | AC2                 |
| T3, T5      | AC3, AC4            |
| T4, T6      | AC1, AC3, AC5       |
| T7          | AC1, AC5, AC6, AC7  |
| T8          | AC8                 |
| T9          | AC1, AC2, AC3, AC4, AC5, AC8 |
| T10         | AC5, AC6, AC7       |
| T11         | (supports AC5 env)  |
| T-DoD       | AC1, AC3, AC9 + manual AC2 |

_Every AC is covered: AC1 (T4/T6/T7/T9), AC2 (T1/T2/T9 + manual T-DoD), AC3 (T3/T5/T9), AC4 (T3/T5/T9), AC5 (T6/T7/T9/T10), AC6 (T7/T10), AC7 (T7/T10), AC8 (T8/T9), AC9 (T-DoD)._
